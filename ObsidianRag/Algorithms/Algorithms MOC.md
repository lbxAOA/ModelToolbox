---
category: Index
sources: [OI Wiki (oi-wiki.org)]
generated_at: 2026-07-06T06:48:36.577870+00:00
generated_by: claude
auto_generated: true
---

# Algorithms MOC

A structured reference of competitive-programming algorithms and data structures, organized top-down: each domain (parent) represents the problem/function it addresses, and each linked note (child) is a specific algorithm belonging to it. Every leaf note includes a **Problem Recognition Signals** section — phrased like real problem statements and constraints — to help classify an unseen OI problem into the algorithm class it likely needs.

## Domains
- [[Algorithm Fundamentals MOC]] — enumeration, recursion, greedy, sorting, prefix sums, binary search/lifting
- [[Search MOC]] — DFS/BFS, heuristic search (A*, IDA*), backtracking, adversarial search
- [[Dynamic Programming MOC]] — knapsack, interval/tree/bitmask/digit DP, DP optimizations
- [[Strings MOC]] — pattern matching, hashing, tries, suffix structures, palindromes
- [[Mathematics MOC]] — number theory, combinatorics, polynomial transforms, linear algebra, game theory
- [[Data Structures MOC]] — stacks/queues/heaps, Fenwick/segment trees, balanced BSTs, persistent structures
- [[Graph Theory MOC]] — shortest paths, MST, connectivity, flow, matching, tree algorithms
- [[Computational Geometry MOC]] — convex hull, closest pair, half-plane/segment intersection, sweep line
- [[Miscellaneous MOC]] — two pointers, Mo's algorithm, CDQ divide and conquer, randomized techniques

## How to Use This Vault
Start from the domain MOC that matches a problem's apparent shape, then drill into its sub-categories (e.g. Graph Theory MOC → Shortest Path → Dijkstra's Algorithm) to reach the specific algorithm. Use semantic search (`search_obsidian`) with a problem's constraints and phrasing to surface the most likely algorithm notes directly via their Problem Recognition Signals.
