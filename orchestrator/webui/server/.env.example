# 复制为 .env 并按需修改（不会被提交，见根 .gitignore）

# HTTP 端口
PORT=5678

# 监听地址，默认只监听本机，避免局域网暴露管理面板
HOST=127.0.0.1

# 仓库根目录，默认自动推断（server/src 向上 4 级），一般不需要修改
# MODELTOOLBOX_ROOT=C:\ModelToolbox

# 调用各模块 Python CLI 时使用的解释器
# PYTHON_BIN=python
